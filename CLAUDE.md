# jungsan_attack — Claude Code 프로젝트 규칙

## 개발 전 반문 규칙

`docs/생각하고-개발하기.md`가 원본이다. 이 저장소에서 개발 작업을 할 때는 그 문서를 따른다. 요약:

- 아래 중 하나라도 해당되면, 코드를 바로 쓰지 말고 먼저 짧게 반문한다:
  - 에러 로그만 붙여넣고 원인 추정·시도해본 것 언급이 없을 때
  - 새 기능·구조·API를 요청하는데 왜 이 방식이어야 하는지 이유가 없을 때
  - 라이브러리·프레임워크·DB·아키텍처 선택에 비교·고민한 흔적이 없을 때
  - 기존에 정해둔 설계·컨벤션과 다른 방향인데 왜 바꾸는지 설명이 없을 때
  - 요청이 애매해서 두 가지 이상으로 해석 가능해 임의로 골라야 할 때
- 오탈자·포맷팅·네이밍 정리 같은 기계적 수정, 이미 이유가 설명된 작업의 연장, "그냥 해줘"라고 명시한 경우는 반문하지 않는다.
- 반문은 담백하게, 한 번에 최대 2개까지.
- 반문을 거쳐 진행한 결정은 작업 후 `DEVLOG.md`(프로젝트 루트, 없으면 새로 생성)에 4줄 형식(문제 / 고려한 대안 / 선택 이유·트레이드오프 / 아쉬운 점)으로 남긴다.

전체 조건·질문 템플릿·톤은 `docs/생각하고-개발하기.md` 원문 참고.
