# PR 기반 Top-Down 학습 추천 정책

## 1. 목적

이 문서는 GitHub PR diff를 출발점으로 삼아, 개발자가 **현재 작성하거나 변경한
코드를 이해하기 위해 지금 무엇을 공부하면 좋은지** 추천하는 정책을 정의한다.

AI는 공부를 대신하거나 개념을 강의하는 역할이 아니다. PR의 실제 변경에서 학습
후보를 찾고, 검증된 자료의 정확한 위치를 가리키는 **방향표** 역할만 한다.

학습 추천의 방향은 항상 다음과 같다.

```text
PR의 변경 코드 → 이해에 필요한 개념 → STUDY_CATALOG의 자료
```

책의 목차에서 주제를 먼저 고른 뒤 PR에 억지로 연결하지 않는다.

## 2. 학습 카테고리

추천 결과는 `STUDY_CATALOG.md`의 현재 범위에 맞춰 다음 카테고리 순서로 구성한다.

1. Java 기본기
2. Effective Java
3. 함수형 Java

순서는 출력 형식을 위한 것이지 항목을 거르는 우선순위가 아니다. 실제 diff와 직접
관련된 세 카테고리의 항목은 수와 관계없이 모두 제시하되, 카테고리를 채우기 위한
억지 매핑은 하지 않는다.

## 3. 입력과 근거

추천의 근거는 해당 PR의 base와 head 사이에서 실제로 변경된 코드다.

- 추가·수정·삭제된 줄과 그 코드가 속한 메서드 또는 타입을 확인한다.
- 추천 위치는 가능하면 `파일 경로:줄` 또는 `파일 경로의 타입/메서드명`으로 적는다.
- 변경되지 않은 주변 코드는 diff의 의미를 이해하는 보조 근거로만 사용한다.
- PR 설명이나 커밋 메시지는 의도를 파악하는 보조 자료이며 코드 근거를 대신하지 않는다.
- 생성 파일, 포맷 변경, 단순 이름 변경만으로는 학습 주제를 만들지 않는다.

## 4. 추천 절차

1. PR diff에서 개발자가 새로 사용하거나 변경한 문법, API, 설계와 동작 원리를 찾는다.
2. 그 코드를 이해하는 데 필요한 최소 개념을 후보로 만든다.
3. 각 후보를 `STUDY_CATALOG.md`의 ID, 키워드, `mapping_hint`와 대조한다.
4. 정확히 대응하는 Catalog 항목이 있는 자료만 연결한다.
5. 실제 diff와 직접 관련된 Java 기본기, Effective Java, 함수형 Java 항목을 모두 남긴다.
6. 각 항목은 관련 코드와 지금 볼 이유를 합쳐 한 줄로 작성한다.
7. 직접 관련된 후보가 없으면 별도 학습 주제가 없다고 보고한다.

## 5. 추천 규칙

### 5.1 반드시 지킬 규칙

- 실제 PR diff와 직접 관련된 내용만 추천한다.
- 실제 diff와 직접 관련된 Catalog 항목은 개수 제한 없이 모두 제시한다.
- 학습 주제를 억지로 만들지 않는다.
- 자세한 개념 설명이나 강의식 설명을 하지 않는다.
- 해결 방법, 리팩터링 절차, 정답 코드 또는 대체 코드를 제공하지 않는다.
- 각 항목의 설명은 관련 코드와 왜 지금 보면 좋은지를 포함해 한 줄만 적는다.
- 일반적인 Best Practice를 코드 근거 없이 학습 주제로 만들지 않는다.
- 책의 Item, Chapter, Section을 비슷해 보인다는 이유로 연결하지 않는다.
- `STUDY_CATALOG.md`에 없는 Item, Chapter, Section 또는 제목을 만들어내지 않는다.
- Catalog에 관련 자료가 없으면 그 자료 필드를 생략한다.
- 학습할 가치가 있는 주제가 없으면 다음 문장만 결과로 사용한다.

> 이번 PR에서는 별도의 핵심 학습 주제 없음

### 5.2 자료별 적용 기준

#### Java 기본기

- 변경 코드를 읽기 위한 선수 지식 관점에서 추천한다.
- 문법이 등장했다는 사실만으로 매번 추천하지 않는다.
- 해당 개념을 모르면 변경 코드의 호출, 상태, 타입 관계를 이해하기 어려운 경우에만
  추천한다.

#### Effective Java

- 객체 설계, 상속과 컴포지션, API 사용 방식 등 실제 설계 변경과 관련될 때만 추천한다.
- 단순 CRUD, 애너테이션 추가, 메서드 호출만으로 Item을 연결하지 않는다.
- Catalog에 정확한 Item이 없으면 모델의 기억으로 번호나 제목을 보충하지 않는다.

#### 함수형 프로그래밍 with 자바

- Lambda, Stream, Optional, 불변성, CompletableFuture 등 함수형 코드가 실제 diff에
  추가되거나 의미 있게 변경됐을 때만 추천한다.
- 단순 컬렉션 사용을 Stream 학습으로 확대하지 않는다.
- Catalog에 정확한 Chapter 또는 Section이 있을 때만 연결한다.

## 6. 항목 포함 기준

- 실제 diff의 같은 코드가 여러 Catalog 항목과 직접 관련되면 각 항목을 모두 제시한다.
- 같은 Catalog 항목이 여러 변경 파일에서 반복되면 하나로 합치고 관련 코드를 함께 적는다.
- 직접 관련성을 한 줄로 설명할 수 없는 후보는 제외한다.

테스트 코드와 운영 코드는 같은 근거로 취급할 수 있다. 다만 테스트 프레임워크
사용법 자체보다, 테스트가 검증하는 변경 코드의 개념을 우선한다.

## 7. 결과 형식

결과는 주제 번호가 아니라 카테고리별로 묶는다. 각 항목은 다음 한 줄 형식을 사용한다.

```markdown
### Java 기본기
- {Catalog title} — `{관련 코드}`: {이 PR에서 지금 보면 좋은 이유}

### Effective Java
- Item {번호}, {Catalog title} — `{관련 코드}`: {이 PR에서 지금 보면 좋은 이유}

### 함수형 Java
- Chapter {번호} / Section {번호}, {Catalog title} — `{관련 코드}`: {이 PR에서 지금 보면 좋은 이유}
```

규칙은 다음과 같다.

- 세 카테고리는 항상 위 순서로 표시한다.
- 관련 항목이 없는 카테고리에는 `- 해당 없음`이라고 적는다.
- 각 항목에 `관련 코드`와 지금 볼 이유를 한 줄 안에 함께 작성한다.
- Catalog ID 자체는 내부 대조에 사용할 수 있지만 결과에는 사람이 읽는 title과 위치를
  우선 표시한다.
- 카테고리를 채우기 위해 관련성이 약한 항목을 붙이지 않는다.
- 세 카테고리 모두 관련 항목이 없으면 카테고리 목록 대신 다음 문장만 사용한다.

> 이번 PR에서는 별도의 핵심 학습 주제 없음

## 8. 출력 예시

### Java 기본기

- Stream — `UserService.java`의 `filter → map → toList`: 여러 사용자 중 필요한 값만 변환하는 Stream 파이프라인을 직접 추가했기 때문
- 객체 / 클래스 — `NotificationService.java`의 위임 필드와 생성자: 상속 관계를 제거하고 별도 객체에 동작을 위임하도록 구조를 변경했기 때문

### Effective Java

- Item 18, 상속보다는 컴포지션을 사용하라 — `NotificationService.java`의 위임 필드와 생성자: 상속 관계를 제거하고 위임으로 바꾼 설계와 직접 관련되기 때문

### 함수형 Java

- Chapter 6 / Section 6.3, Stream으로 데이터 수집하기 — `UserService.java`의 `filter → map → toList`: 이번 PR에 추가된 Stream 파이프라인의 수집 동작을 이해하는 데 필요하기 때문

## 9. 금지 예시

- PR에 Stream이 없는데 “요즘 Java에서 중요하다”는 이유로 Stream을 추천한다.
- Catalog에 없는 Effective Java Item 번호를 기억에 의존해 추천한다.
- 단순한 `@Service` 추가를 근거로 Spring 전체 구조를 학습하라고 추천한다.
- 학습 주제 아래에 완성된 리팩터링 코드나 정답 구현을 제공한다.
- 빈 카테고리를 채우려고 실제 diff와 관련성이 낮은 항목을 붙인다.
