# DEVLOG

`docs/생각하고-개발하기.md` 규칙에 따라, 반문을 거쳐 내린 결정을 남긴다.
블로그·포트폴리오·면접 답변 재료로 그대로 재활용한다.

---

## [2026-09-13] 개발 스프린트를 1개월에서 2~3개월로 늘린다

- **문제:** `REQUIREMENTS.md`가 v2로 확장되며 범위가 늘었다 — 영수증 이미지
  첨부·OCR·AI 주류 항목 분류가 후속에서 MVP로 들어왔고, 이의제기 1:1 채팅도
  MVP에 그대로 남았다. `ADR-014`와 2026-08-24 DEVLOG 항목이 전제한 "MVP 기능
  자체는 한 달이면 만들 수 있는 규모"가 더는 맞지 않는다.
- **고려한 대안:** ① 1개월을 유지하고 OCR·AI 보조 입력을 다시 후속으로 뺀다
  ② 기간을 2~3개월로 늘리고 범위를 유지한다
- **선택 이유 / 트레이드오프:** ②. 계산 엔진뿐 아니라 OCR·AI 연동 경험도
  포트폴리오 가치가 있다고 판단해 범위를 유지하기로 했다. 대신 `ADR-014`가
  근거로 든 "개발 1개월 / 역량 6개월" 트랙 구분이 흐려진다 — 개발 기간이 늘어난
  만큼 6개월 역량 트랙 도중에 실제 트래픽·운영 신호(`ADR-014` 재검토 조건)를
  더 일찍 만날 가능성도 같이 커진다.
- **영향받는 문서:** `ARCHITECTURE.md`의 "1개월 MVP 스프린트" 표현을 갱신했다.
  `ADR-014` 본문과 2026-08-24 DEVLOG 항목은 그 시점의 결정 기록이므로 고치지
  않고 그대로 남겨둔다.

---

## [2026-09-12] MySQL 컬럼 제약과 주석은 한 ALTER에서 함께 고정한다

- **문제:** Liquibase의 `addNotNullConstraint`는 MySQL에서 기존 컬럼 주석을 지웠고,
  이를 복구한 `setColumnRemarks`는 반대로 `NOT NULL`을 풀었다. YAML은 정상 적용됐지만
  최종 스키마가 선언 의도와 달라져 `information_schema` 대조에서 발견됐다.
- **고려한 대안:** ① 적용된 changeSet을 수정 ② 제약과 주석을 별도 Liquibase change로 반복
  ③ 후속 changeSet의 단일 MySQL `ALTER TABLE ... MODIFY COLUMN`에서 둘을 함께 선언
- **선택 이유 / 트레이드오프:** ③. 이미 실행한 changeSet의 checksum을 보존하면서 최종
  컬럼 정의를 원자적으로 고정한다. 대신 해당 보정 changeSet은 MySQL 전용 SQL이 된다.
- **아쉬운 점:** `013`은 문제를 완전히 고치지 못한 중간 이력으로 남는다. 다만 삭제하거나
  고치면 적용 DB의 checksum을 깨므로 `014`가 최종 정의를 보장하는 과정을 그대로 보존한다.

## [2026-09-03] 확정된 송금 명세는 불변 스냅샷으로 저장한다

- **문제:** 기존 ADR-005는 계산 결과를 저장하지 않고 참여자 하나에 입금 상태를 뒀다.
  Core v2에서는 한 참여자가 여러 차수 총무에게 각각 송금하며, 금액 확정 후 입력을
  바꾸지 않는다. 계산값을 전혀 저장하지 않으면 입금 확인과 이의제기가 어느 확정 금액을
  대상으로 했는지 안정적으로 가리킬 수 없다.
- **고려한 대안:** ① 모든 조회에서 Core를 다시 실행하고 참여자에 입금 상태 유지
  ② 계산 상세 전체를 영구 저장 ③ 미리보기는 재계산하되 확정 시 송금 명세만 저장
- **선택 이유 / 트레이드오프:** ③. 계산의 유일한 주체는 Core로 유지하면서 입금·분쟁의
  증거인 `sender → recipient → amount`만 고정한다. 저장 데이터는 늘지만, 확정 이후의
  상태 전이와 감사 이력이 명확해지고 여러 수취인을 표현할 수 있다.
- **아쉬운 점:** ADR-005의 제목과 일부 기존 스키마가 새 결정과 충돌한다. 다음 migration
  전에 후속 ADR을 쓰고, `participants.payment_status` 중심 모델을 transfer 중심으로 옮겨야 한다.

## [2026-09-01] 차수별 면제와 수취인별 1원 올림으로 Core v2를 단순화한다

- **문제:** 기존 전역 면제는 잠깐 참석한 차수만 비용을 면제하는 상황을 표현하지 못했고,
  10원·100원 올림은 소액에서 총무 부담을 음수로 만들며 사용자 기대보다 큰 차이를 냈다.
- **고려한 대안:** ① 전역 면제와 선택형 10·100원 올림 유지 ② 차수별 면제와 1원 올림,
  총무별 직접 송금 ③ 나머지 배분 알고리즘으로 모든 사람의 정수 금액을 별도 최적화
- **선택 이유 / 트레이드오프:** ②. 참여 상태를 `ABSENT·EXEMPT·SOBER·DRANK`로 닫고,
  동일 총무의 정확한 원부담을 합친 뒤 한 번만 1원 올림한다. 계산과 설명은 단순해졌지만
  서로 다른 총무 사이의 반대 방향 송금도 상계하지 않아 송금 건수는 늘 수 있다.
- **아쉬운 점:** 면제된 총무가 대신 결제하면 본인이 잔액을 조정할 수 없어 비면제 부담자
  한 명을 결정론적으로 조정자로 골라야 한다. 드문 초소액에서는 그 금액도 음수가 될 수 있어
  단위 강등 대신 명시적 검증 실패로 처리한다.

## [2026-08-29] 정산 책임을 술자리 전체 주최자에서 차수별 총무로 옮긴다

- **문제:** 기존 명세는 한 명의 주최자가 모든 차수와 입금을 관리했지만, 실제 술자리에서는
  1차·2차 결제자가 다를 수 있다. 전역 상계는 송금 횟수는 줄여도 각 결제자가 자기 차수의
  비용을 돌려받는다는 사용자의 기대와 어긋났다.
- **고려한 대안:** ① 기존처럼 전체 주최자 한 명과 전역 상계 유지 ② 전체 주최자와 차수별
  결제자를 함께 둠 ③ 전체 총무 없이 차수마다 결제자이자 수취인인 총무를 한 명씩 둠
- **선택 이유 / 트레이드오프:** ③. 같은 총무가 맡은 차수만 수취 금액을 합치고 서로 다른
  총무의 채권은 상계하지 않는다. 실제 결제 관계가 결과 화면과 송금 방향에 그대로 드러나는
  대신, 총무가 여러 명이면 참여자의 송금 횟수가 늘어난다.
- **아쉬운 점:** 현재 core 엔진과 `CALC_RULES.md`는 대표결제자와 전역 greedy 상계를 전제로
  이미 완성돼 있다. 새 규칙은 테스트 기대값과 반올림 귀속까지 다시 검증해야 하므로 제품
  요구사항을 먼저 고정하고 기술 문서와 엔진은 별도 단계에서 개정한다.

## [2026-08-23] MVP를 MSA가 아니라 모놀리스로 되돌린다 (ADR-014)

- **문제:** `ADR-013`이 Spring Cloud + k3s MSA를 "확정"으로 적고 `ADR-006`(단일 VM)을
  대체한 상태였다. 그런데 그 전환 근거가 *"학습 목적"* 이었다 — 실제로 아픈 곳이
  생겨서가 아니었다. 사용자도 트래픽도 팀도 없는 MVP 단계에서 운영 대상만 늘어난다.
- **고려한 대안:**
  ① `ADR-013` 그대로 MSA 유지 — 학습 목적임을 인정하고 진행
  ② 모놀리스로 되돌리고 `ADR-013`을 완전히 폐기
  ③ 모놀리스로 되돌리되 `ADR-013`을 **보류** 상태로 살려둔다
- **선택 이유 / 트레이드오프:** ③. "구조를 위한 프로젝트가 되면 안 된다 — 모놀리식부터
  하다가 문제가 발견되면 그때 바꾼다"가 다른 ADR들이 지켜온 방식(`002` 아웃박스,
  `006` K8s 배제, `012` ES 유예)과도 일치한다. ②가 아니라 ③인 이유는, MSA 설계·서비스
  경계·메모리 예산 계산이 `013`에 이미 다 있어서 **발동 조건이 오면 새로 고민할 필요가
  없기 때문**이다. 대가로 Spring Cloud·k3s 학습이 미뤄진다.
- **아쉬운 점:** `ADR-013`을 쓸 때 "학습 목적"이라고 **문서에 정직하게 적어둔 덕분에**
  이 문제를 나중에라도 발견할 수 있었다. 반대로 말하면, 그때 "신호 → 구조" 순서를
  지켰다면 애초에 안 뒤집었을 결정이다. 앞으로 ADR을 쓸 때 근거 자리에 "배우고 싶어서"가
  들어가면 그 자체가 경고 신호다.

## [2026-08-24] 개발은 1개월 스프린트, 역량은 6개월 트랙으로 분리한다

- **문제:** `ADR-014`에서 "학습은 월 단위로 순차 진행"이라고만 적어둬서, 개발 일정과
  학습 일정이 한 덩어리로 뭉쳐 있었다. 이대로면 기능 개발이 학습 속도에 끌려간다.
- **고려한 대안:** ① 개발·학습을 같이 6개월에 걸쳐 진행 ② 개발을 1개월에 몰아치고
  학습은 별도 트랙으로 6개월
- **선택 이유 / 트레이드오프:** ②. 근거 네 가지 —
  (1) 가용 시간이 한정적이라 초고효율로 써야 한다
  (2) MVP 기능 자체는 한 달이면 만들 수 있는 규모다. 대신 그 위에서 6개월간 지지고
      볶을 소재는 충분하다
  (3) 늘어뜨리면 처진다. 애자일하게 짧은 주기로 굴려야 한다
  (4) 처지면 재미가 떨어진다. 매 순간 판단력을 늘리려면 결정 빈도가 높아야 한다
  대가: 한 달 안에 밀어붙이므로 **완성도보다 동작을 우선**하게 된다. 리팩터링 부채가
  쌓일 것이고, 그 부채를 갚는 과정 자체가 6개월 트랙의 소재가 된다.
- **6개월 트랙에서 극대화할 것:** STAR 기록, 코드 리뷰, DDD. 이 셋은 짧게 배울 수
  없고 반복이 필요하다. 기술 학습은 탑다운·바텀업을 혼합한다.
- **아쉬운 점:** 1개월 안에 끝낸다는 건 **`ADR-014`의 재검토 조건이 발동될 만큼의
  트래픽·문제를 그 기간에 못 만난다**는 뜻이기도 하다. MSA·채팅 분리 같은 결정은
  6개월 트랙에서 "실제 신호"가 아니라 "의도적 실험"으로 다뤄야 할 수 있다 —
  그때 가서 정직하게 구분해서 기록할 것.

## [2026-08-23] 패키지는 layer가 아니라 feature로 나눈다 (ADR-014)

- **문제:** 모놀리스로 가면 내부 구조를 어떻게 잡을지 정해야 한다. 기존 실무 경험은
  `controller/`·`service/`·`repository/`로 나누는 layer 방식이었다.
- **고려한 대안:** ① package by layer(익숙함) ② package by feature
- **선택 이유 / 트레이드오프:** ②. 나중에 `chat/`을 통째로 뽑아 별도 프로세스로
  옮겨야 하는데(`ADR-010` 발동 시), feature 패키지면 폴더 하나를 들어내면 되지만
  layer 방식이면 세 폴더에서 채팅 관련 파일만 골라내야 한다. **모놀리스로 시작하되
  분리 가능성을 열어두려면 feature 쪽이 맞다.** 대가는 같은 역할끼리 한눈에 비교하기가
  조금 불편해지는 것.
- **아쉬운 점:** 실제로는 이미 `gathering/`·`user/`·`config/`로 그렇게 잡혀 있었는데,
  그게 의식적 결정이 아니라 자연스럽게 그렇게 된 것이었다. 지금 문서로 명시했다.
